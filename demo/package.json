{
	"name": "demo",
	"scripts": {
		"build": "mkdir -p public && cp -v -r ../crate/pkg public/pkg && vite build && ls -l dist/pkg/procedural_tamago_sprite_bg.wasm",
		"start": "vite --host"
	},
	"devDependencies": {
		"crumbs-design-system": "^1.0.0",
		"vite": "^5.3.1"
	}
}
