{
	"name": "demo",
	"scripts": {
		"build": "mkdir -p public && cp -r ../crate/pkg/* public && vite build",
		"start": "vite --host"
	},
	"devDependencies": {
		"crumbs-design-system": "^1.0.0",
		"vite": "^5.3.1"
	}
}
